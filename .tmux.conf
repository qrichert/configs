# Turn on mouse.
set -g mouse on

# Better colors.
set -g default-terminal "screen-256color"

# Hide status bar.
#set -g status off

# Use Vim keys.
setw -g mode-keys vi

# Increase history limit.
set-option -g history-limit 5000
